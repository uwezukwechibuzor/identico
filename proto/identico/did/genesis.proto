syntax = "proto3";
package identico.did;

option java_multiple_files = true;
option go_package = "github.com/uwezukwechibuzor/identico/x/did/types";

import "identico/did/v2/did.proto";

// GenesisState defines the did module's genesis state.
message GenesisState {
  map<string, DIDDocumentWithSeq> documents = 1;
}